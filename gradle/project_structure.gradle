project(':backend') {
  dependencies {
    compile project(':base')
    compile project(':corelib')
    compile project(':ir')
  }
}

project(':base') {
  /* No Dependencies */
}

project(':ir') {
  dependencies {
    compile project(':base')
  }
}

project(':corelib') {
  /* No Dependencies */
}

project(':frontend') {
  dependencies {
    compile project(':base')
    compile project(':corelib')
    compile project(':ir')
  }
}

project(':test') {
  dependencies {
    compile project(':backend')
    compile project(':base')
    compile project(':corelib')
    compile project(':frontend')
  }
}
