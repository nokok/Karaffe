project(':ast') {
  dependencies {
    compile project(':base')
  }
}

project(':backend') {
  dependencies {
    compile project(':base')
    compile project(':corelib')
    compile project(':ir')
  }
}

project(':base') {
}

project(':ir') {
}

project(':corelib') {
}

project(':frontend') {
  dependencies {
    compile project(':ast')
    compile project(':base')
    compile project(':corelib')
    compile project(':ir')
  }
}

project(':test') {
  dependencies {
    compile project(':ast')
    compile project(':backend')
    compile project(':base')
    compile project(':corelib')
    compile project(':frontend')
  }
}
